#set($conversationID = $ctx.stash.get('conversationID'))
#if(!$conversationID)
  #return
#end

{
  "version": "2018-05-29",
  "operation": "GetItem",
  "key": {
    "id": $util.dynamodb.toDynamoDBJson($ctx.stash.get('conversationID'))
  }
}