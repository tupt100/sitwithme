#if(!$ctx.identity.claims["custom:id"])
  $util.error("Unauthorized")
#end

{
  "version": "2018-05-29",
  "operation": "GetItem",
  "key": {
    "id": $util.dynamodb.toDynamoDBJson($ctx.identity.claims["custom:id"])
  }
}